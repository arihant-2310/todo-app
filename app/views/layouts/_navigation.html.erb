<nav class="navbar navbar-expand-lg">
  <div class="container-fluid">
    <%= link_to "Task Manager", root_path, class: "navbar-brand mb-0 h1", id: "logo" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <% if user_signed_in? %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle fs-5 text-white-50" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Profile
            </a>
            <ul class="dropdown-menu">
              <%= link_to "View", root_path, class: "dropdown-item" %>
              <%= button_to "Logout", destroy_user_session_path, data: {turbo: "false"}, method: :delete, class: "btn btn-sm dropdown-item" %>
            </ul>
          </li>

          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle fs-5 text-white-50" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Tasks
            </a>
            <ul class="dropdown-menu">
              <%= link_to "View", tasks_path, class: "dropdown-item" %>
              <%= link_to "New", new_task_path, class: "dropdown-item" %>
            </ul>
          </li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
